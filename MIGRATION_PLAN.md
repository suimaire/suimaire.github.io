---
title: 과학 수업 메인 포털 전환 계획
nav_exclude: true
search_exclude: true
---

# 과학 수업 메인 포털 전환 계획

## 목표와 안전 원칙

`suimaire.github.io`를 여러 과학 수업을 한곳에서 찾는 메인 포털로 확장한다. 기존 생물정보학 수업은 검증이 끝나기 전까지 이동하거나 삭제하지 않으며, 공개된 주소와 수업 흐름을 우선 보존한다.

이 계획의 작업 범위는 `suimaire.github.io` 저장소로 한정한다. 별도 저장소에서 개발 중인 생태계 시뮬레이션 소스는 이 작업에서 가져오거나 수정하지 않는다.

## 현재 콘텐츠 기준선

| 구분 | 현재 위치 | 보존 기준 |
| --- | --- | --- |
| 생물정보학 수업 | `lectures/day1.md`–`day5.md` | 본문과 공개 URL 유지 |
| 아미노산 이미지 | `assets/img/amino/`의 PNG 20개 | 파일명과 경로 유지 |
| 공통 스타일 | `assets/css/`, `_sass/custom/` | 강의 가독성이 달라지지 않도록 분리 |
| 공통 스크립트 | `assets/js/heading-numbers.js` | 기존 강의에서 계속 로드 |
| 사이트 생성 방식 | Jekyll + `just-the-docs` 원격 테마 | 1차 개편에서는 유지 |

확인된 기존 과제 한 가지가 있다. `lectures/day5.md` 마지막의 Google Classroom 링크 두 개는 실제 주소가 아닌 자리표시자다. 포털 전환과 섞어 고치지 않고, 실제 수업 링크를 확인한 뒤 별도 수정한다.

## 단계별 이전 순서

### 0. 기준선과 복구 지점 만들기 — 완료

1. 개편 직전 커밋에 복구용 태그를 만든다.
2. Day 1–5 공개 주소, 이미지 20개, 주요 외부 링크 목록을 저장한다.
3. 현재 GitHub Pages 화면과 모바일 화면을 기준 자료로 남긴다.

완료 조건: 한 번의 되돌리기로 현재 생물정보학 사이트를 복구할 수 있다.

개편 직전 커밋 `b70abe1`에 `pre-science-portal-2026-09-01` 복구 태그를 만들었다.

### 1. 포털을 기존 콘텐츠 위에 얹기 — 완료

1. 홈 화면만 과학 수업 포털로 교체한다.
2. `/bioinformatics/` 안내 페이지를 추가하고 기존 Day 1–5 문서로 연결한다.
3. 새 디자인은 `.science-portal`, `.course-overview` 아래로 범위를 제한해 기존 강의 스타일과 충돌하지 않게 한다.
4. 기존 강의 문서와 이미지에는 손대지 않는다.

완료 조건: 새 홈에서 모든 기존 수업에 도달할 수 있고, 기존 주소도 직접 열 수 있다.

### 2. 정보 구조 정리 — 완료

1. 사이드바에서 Day 1–5를 `생물정보학 기초` 아래에 묶는다.
2. 이때는 문서의 위치를 옮기지 않고 머리말의 상위 메뉴 정보만 추가한다.
3. 새 수업에는 고유한 폴더와 안내 페이지를 사용한다.

완료 조건: 메뉴 구조가 바뀌어도 기존 즐겨찾기와 수업 링크가 모두 작동한다.

승인 후 Day 1–5 문서의 본문과 위치는 유지하고, 머리말에 `생물정보학 기초` 상위 메뉴 정보만 추가했다.

### 3. 콘텐츠 표준화 — 별도 검토

1. 각 수업에 대상, 준비물, 학습 목표, 활동, 제출 항목을 같은 순서로 표시한다.
2. 외부 서비스 링크와 데이터 접근 코드를 다시 확인한다.
3. 오래된 표현이나 자리표시자 링크는 교사 확인을 받은 항목만 수정한다.

완료 조건: 내용 변경 목록을 따로 검토할 수 있고, 포털 디자인 변경과 섞이지 않는다.

### 4. 실제 이동 또는 정리 — 필요할 때만

문서 위치를 바꿔야 한다면 먼저 새 위치에 복사하고, 예전 위치에는 영구 이동 안내를 둔다. 최소 두 번의 수업 운영 주기 동안 접근 기록과 오류를 확인한 뒤에만 중복 파일 제거를 검토한다. 삭제는 별도 승인 없이는 진행하지 않는다.

## 검증 체크리스트

- `/`, `/bioinformatics/`, `/lectures/day1.html`–`day5.html`이 모두 열린다.
- Day 1의 아미노산 이미지 20개가 표시된다.
- 홈의 모든 수업 링크와 기존 외부 링크가 키보드로 접근 가능하다.
- 좁은 화면에서 카드, 표, 코드 블록이 잘리거나 겹치지 않는다.
- GitHub Pages 빌드가 성공하고 브라우저 콘솔에 차단 오류가 없다.
- 강의 문서 5개의 내용 차이가 예상된 머리말 변경 외에는 없다.

## 되돌리기 방법

1차 포털 초안은 홈, 설정 제목, 공통 헤더의 스타일 연결, 새 CSS, 새 강좌 안내 페이지만 변경한다. 문제가 생기면 이 파일들만 직전 버전으로 되돌리면 기존 생물정보학 수업으로 즉시 복구된다. `lectures/`와 `assets/img/amino/`는 1차 단계에서 수정하지 않으므로 별도 콘텐츠 복구가 필요 없다.

## 승인 후에도 의도적으로 하지 않은 일

- 기존 Day 1–5 문서 이동·이름 변경·삭제
- 생태계 시뮬레이션 저장소 연결 또는 수정
- 실제 Google Classroom 주소 추정
